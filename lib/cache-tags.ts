/** Single tag for all portfolio / CMS-backed reads cached via `unstable_cache`. */
export const CACHE_TAG_PORTFOLIO = 'portfolio-data';

/** Default TTL for cached DB reads (seconds). */
export const PORTFOLIO_CACHE_REVALIDATE_SECONDS = 120;
